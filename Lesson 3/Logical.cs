using System.Threading.Channels;

int age = 20; bool hasId = false;

bool isMember = true; bool hascoupon = false; bool is ChannelClosedException = true;

bool canEnter = age >= 18 && hasId;

bool discount = !isClosed;
